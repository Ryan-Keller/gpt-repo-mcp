export const HERMES_WATCH_WIDGET_URI = "ui://widget/hermes-watch-v4.html";

export function hermesWatchWidgetHtml(): string {
  return String.raw`<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<style>
:root{color-scheme:dark;--bg:#0d1112;--panel:#171d1d;--panel2:#202827;--ink:#edf2ee;--muted:#91a099;--line:#2c3633;--green:#7ee2a8;--amber:#f3c969;--red:#ff8e87;--blue:#82c7ff}*{box-sizing:border-box}body{margin:0;background:linear-gradient(145deg,#0b0f10,#161d1b);color:var(--ink);font:13px/1.45 ui-monospace,SFMono-Regular,Menlo,Consolas,monospace}.shell{padding:16px;min-height:420px}.head{display:flex;align-items:flex-start;justify-content:space-between;gap:14px;border-bottom:1px solid var(--line);padding-bottom:12px}.eyebrow{color:var(--muted);font-size:10px;letter-spacing:.14em;text-transform:uppercase}.title{font:650 20px/1.2 ui-sans-serif,system-ui;margin-top:3px}.beacon{display:flex;align-items:center;gap:8px;color:var(--green);font-size:11px}.dot{width:9px;height:9px;border-radius:50%;background:currentColor;box-shadow:0 0 14px currentColor;animation:pulse 1.8s infinite}@keyframes pulse{50%{opacity:.35}}.status{background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:12px;margin:14px 0}.operator{font:500 14px ui-sans-serif,system-ui}.meta,.hint{color:var(--muted);font-size:10px;margin-top:5px}.counts{display:flex;gap:7px;flex-wrap:wrap}.count{border:1px solid var(--line);border-radius:999px;padding:4px 8px;color:var(--muted)}.count b{color:var(--ink)}.board{display:grid;grid-auto-flow:column;grid-auto-columns:minmax(190px,1fr);gap:10px;overflow-x:auto;padding:14px 0}.column{background:rgba(23,29,29,.72);border:1px solid var(--line);border-radius:10px;min-height:130px}.column h3{font:600 11px ui-sans-serif,system-ui;text-transform:uppercase;letter-spacing:.08em;color:var(--muted);margin:0;padding:9px 10px;border-bottom:1px solid var(--line)}.cards{padding:8px}.card{width:100%;text-align:left;background:var(--panel2);border:1px solid transparent;border-radius:8px;padding:9px;margin:0 0 7px;color:var(--ink);cursor:pointer}.card:hover,.card.selected{border-color:#4c7462}.card-title{font:600 12px/1.35 ui-sans-serif,system-ui}.card-meta{display:flex;justify-content:space-between;gap:6px;color:var(--muted);font-size:9px;margin-top:7px}.card-result{color:#b9c8c1;font-size:9px;margin-top:6px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}.empty{color:var(--muted);padding:10px}.workspace{display:grid;grid-template-columns:minmax(0,1fr) minmax(250px,.42fr);gap:12px}.timeline{border-left:1px solid var(--line);margin:4px 6px 0;padding-left:15px}.event{position:relative;margin:0 0 11px}.event:before{content:"";position:absolute;left:-19px;top:5px;width:7px;height:7px;border-radius:50%;background:var(--blue)}.event time{color:var(--muted);font-size:9px}.inspector{background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:12px}.inspector h2{font:600 15px ui-sans-serif,system-ui;margin:0 0 4px}.fields{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin:10px 0}input,textarea,select{width:100%;background:#0f1414;border:1px solid var(--line);border-radius:7px;color:var(--ink);padding:7px;font:11px ui-monospace,monospace}textarea{min-height:76px;resize:vertical}.actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:10px}button{appearance:none;border:1px solid var(--line);background:#202726;color:var(--ink);border-radius:8px;padding:7px 9px;font:11px ui-monospace,monospace;cursor:pointer}button.primary{border-color:#3c7255;color:var(--green)}button.warn{border-color:#715c34;color:var(--amber)}button.danger{border-color:#704743;color:var(--red)}button:disabled{opacity:.42;cursor:default}.receipt{min-height:18px;color:var(--muted);font-size:10px;margin-top:8px}.error{color:var(--red)}.ok{color:var(--green)}@media(max-width:720px){.workspace{grid-template-columns:1fr}.board{grid-auto-columns:minmax(170px,80vw)}}
.pfolio{max-width:980px;margin:0 auto;padding:14px;min-height:0;background:#0d1112}.p-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start;padding-bottom:11px;border-bottom:1px solid var(--line)}.p-head h1{font:650 19px/1.2 ui-sans-serif,system-ui;margin:3px 0}.p-sub{color:var(--muted);font-size:10px;max-width:620px}.p-layout{display:grid;grid-template-columns:minmax(0,1fr) minmax(230px,.42fr);grid-template-areas:"overview tray" "actions tray";gap:10px;margin-top:10px}.p-panel{background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:11px;min-width:0}.p-overview{grid-area:overview}.p-action-panel{grid-area:actions}.p-tray{grid-area:tray;align-self:start;position:sticky;top:8px}.p-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:6px;margin-top:9px}.p-stat{background:#101515;border:1px solid var(--line);border-radius:8px;padding:7px}.p-stat b{display:block;font:650 16px ui-sans-serif,system-ui}.p-stat span{color:var(--muted);font-size:9px}.p-lenses{display:flex;gap:5px;overflow:auto;padding:2px 0 5px}.p-lens{white-space:nowrap;border:1px solid var(--line);border-radius:999px;padding:4px 7px;color:var(--muted);font-size:9px}.p-actions{max-height:410px;overflow:auto;overscroll-behavior:contain;padding-right:4px}.p-group{border:1px solid var(--line);border-radius:8px;margin:0 0 7px;background:#111716;overflow:hidden}.p-group>summary{display:flex;align-items:center;justify-content:space-between;gap:8px;padding:9px;cursor:pointer;list-style:none;font:650 11px ui-sans-serif,system-ui}.p-group>summary::-webkit-details-marker{display:none}.p-group>summary:after{content:"＋";color:var(--muted);font:14px ui-monospace,monospace}.p-group[open]>summary:after{content:"−"}.p-group-count{margin-left:auto;color:var(--muted);font:9px ui-monospace,monospace}.p-group-selected{color:var(--green)}.p-group-body{padding:0 9px 3px;border-top:1px solid var(--line)}.p-single{border:1px solid var(--line);border-radius:8px;margin:0 0 7px;padding:0 9px 3px;background:#111716}.p-single-label{display:block;padding:8px 0 2px;color:var(--muted);font-size:9px}.p-action{display:grid;grid-template-columns:18px minmax(0,1fr);gap:7px;border-top:1px solid var(--line);padding:8px 2px;cursor:pointer}.p-action:first-child{border-top:0}.p-action input{margin:2px 0;accent-color:var(--green)}.p-action-title{font:600 11px/1.35 ui-sans-serif,system-ui}.p-action-meta{color:var(--muted);font-size:9px;margin-top:2px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.p-queue{max-height:250px;overflow:auto;margin:8px 0}.p-queued{border-top:1px solid var(--line);padding:6px 0;font-size:10px}.p-toolbar{display:flex;align-items:center;justify-content:space-between;gap:7px;margin-bottom:6px}.p-buttons{display:flex;gap:6px;flex-wrap:wrap}.p-buttons button{min-height:34px}.p-primary{border-color:#3c7255;color:var(--green)}.p-warning{color:var(--amber)}.p-count{font:650 25px ui-sans-serif,system-ui;color:var(--green)}.p-empty{color:var(--muted);padding:10px 0}.p-note{color:var(--muted);font-size:9px;margin:8px 0}.p-receipt{min-height:16px;color:var(--red);font-size:9px;margin-top:5px}@media(max-width:720px){.pfolio{padding:10px}.p-layout{grid-template-columns:1fr;grid-template-areas:"overview" "tray" "actions"}.p-tray{position:static}.p-actions{max-height:340px}.p-stats{grid-template-columns:repeat(3,minmax(0,1fr))}.p-head h1{font-size:17px}.p-buttons button{flex:1 1 auto}.p-queue{max-height:110px}}
.p-action-title,.p-action-meta{display:block}
</style>
</head>
<body><main id="kanbanApp" class="shell">
<header class="head"><div><div class="eyebrow">Shared Agent Bridge</div><div class="title">Hermes guarded Kanban</div></div><div class="beacon"><span class="dot"></span><span id="beacon">awaiting evidence</span></div></header>
<section class="status"><div id="operator" class="operator">Waiting for the first Hermes observation.</div><div id="meta" class="meta">Read-only until you approve a guarded task command.</div></section>
<div id="counts" class="counts"></div><div id="board" class="board"><div class="empty">No Kanban evidence received yet.</div></div>
<section class="workspace"><div><div id="timeline" class="timeline"><div class="empty">No evidence received yet.</div></div></div>
<aside class="inspector"><div class="eyebrow">Selected card</div><h2 id="taskTitle">Choose a Kanban card</h2><div id="taskMeta" class="meta">Card controls use optimistic status checks and require connector approval.</div>
<textarea id="instruction" placeholder="Correction, comment, block reason, scheduling note, or recovery evidence"></textarea>
<div class="fields"><input id="assignee" placeholder="Hermes profile"><select id="blockKind"><option value="needs_input">needs input</option><option value="transient">transient</option><option value="capability">capability</option><option value="dependency">dependency</option></select></div>
<input id="followTitle" placeholder="New follow-up task title">
<div class="actions"><button id="diagnose">Diagnose with ChatGPT</button><button id="correct" class="primary">Send correction</button><button id="comment">Add comment</button><button id="assign">Assign</button><button id="block" class="danger">Block</button><button id="schedule" class="warn">Schedule</button><button id="recover" class="primary">Recover</button><button id="archive" class="danger">Archive</button><button id="create">Create follow-up</button></div><div id="receipt" class="receipt"></div></aside></section>
<div class="actions"><button id="toggle" class="primary">Start resident watch</button><button id="refresh">Observe now</button><button id="review">Ask ChatGPT to review</button></div>
</main><div id="portfolioApp" hidden></div>
<script type="module">
const state={running:false,busy:false,last:null,cursor:"",selected:"",portfolioSelected:new Set()};
const el=id=>document.getElementById(id);const esc=v=>String(v??"").replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));
function toolOutput(){return window.openai?.toolOutput||null}
function tasks(){return state.last?.tasks||[]}function selected(){return tasks().find(t=>t.id===state.selected)||null}
function follow(prompt){if(window.openai?.sendFollowUpMessage){window.openai.sendFollowUpMessage({prompt});return}window.parent.postMessage({jsonrpc:"2.0",method:"ui/message",params:{role:"user",content:[{type:"text",text:prompt}]}},"*")}
function render(d){if(!d)return;if(d.report_id)return renderPortfolioApp(d);el("portfolioApp").hidden=true;el("kanbanApp").hidden=false;state.last=d;state.cursor=d.next_cursor||state.cursor;if(!state.selected&&d.tasks?.length)state.selected=d.tasks[0].id;el("operator").textContent=d.operator_status||"Hermes observation received.";el("beacon").textContent=d.terminal?"terminal":d.heartbeat?"heartbeat":d.changed?"new evidence":"observing";el("meta").textContent=[d.hermes_board||"board",d.state,"polls "+d.poll_count,Math.round((d.elapsed_ms||0)/1000)+"s",new Date(d.observed_at).toLocaleTimeString()].join(" · ");el("counts").innerHTML=(d.board_counts||[]).map(row=>'<span class="count">'+esc(row.status)+' <b>'+Number(row.count)+'</b></span>').join("");renderBoard();renderTimeline();renderInspector();if(d.terminal){state.running=false;buttons()}}
function renderPortfolio(d){state.last=d;document.querySelector(".title").textContent="Portfolio action console";el("beacon").textContent=d.freshness+" · "+d.source_age_days+"d";el("operator").textContent=d.summary;el("meta").textContent="Source snapshot "+(d.source_generated_at||"unknown")+" · select several actions, then route once";el("counts").innerHTML=(d.projects||[]).map(p=>'<span class="count">'+esc(p.name)+' <b>'+esc(p.status)+'</b></span>').join("");el("board").style.gridAutoFlow="row";el("board").style.gridAutoColumns="auto";el("board").innerHTML='<section class="column"><h3>Report lenses</h3><div class="cards">'+(d.sections||[]).map(s=>'<div class="card"><div class="card-title">'+esc(s.topic)+' · '+esc(s.headline)+'</div><div class="card-result">'+esc((s.items||[]).slice(0,4).join(" · "))+'</div></div>').join("")+'</div></section>';el("timeline").innerHTML=(d.actions||[]).map(a=>'<label class="card '+(state.portfolioSelected.has(a.action_id)?'selected':'')+'"><input type="checkbox" data-action="'+esc(a.action_id)+'" '+(state.portfolioSelected.has(a.action_id)?'checked':'')+'><span class="card-title">'+esc(a.title)+'</span><div class="card-meta"><span>'+esc(a.project_name)+'</span><span>'+esc(a.route)+' · '+esc(a.risk)+'</span></div><div class="card-result">'+esc(a.rationale)+'</div></label>').join("")||'<div class="empty">No evidence-derived actions.</div>';el("timeline").querySelectorAll("[data-action]").forEach(box=>box.onchange=()=>{box.checked?state.portfolioSelected.add(box.dataset.action):state.portfolioSelected.delete(box.dataset.action);renderPortfolio(d)});el("taskTitle").textContent=state.portfolioSelected.size+" actions queued";el("taskMeta").textContent="One decision bundle; normal approvals still apply to each mutation.";el("instruction").style.display="none";el("assignee").parentElement.style.display="none";el("followTitle").style.display="none";for(const id of ["diagnose","correct","comment","assign","block","schedule","recover","archive","create"]){el(id).style.display="none"}el("create").parentElement.insertAdjacentHTML("beforeend",'<button id="portfolioAll">Select read-only</button><button id="portfolioClear">Clear</button><button id="portfolioRoute" class="primary">Route selected with ChatGPT</button>');el("portfolioAll").onclick=()=>{for(const a of d.actions||[])if(a.risk==="read_only")state.portfolioSelected.add(a.action_id);renderPortfolio(d)};el("portfolioClear").onclick=()=>{state.portfolioSelected.clear();renderPortfolio(d)};el("portfolioRoute").onclick=()=>{const chosen=(d.actions||[]).filter(a=>state.portfolioSelected.has(a.action_id));if(!chosen.length)return receipt("Select at least one action.",false);follow("Execute this portfolio decision bundle as one orchestrated batch. Validate current evidence for every project, group safe read-only checks, preserve approval boundaries for every mutation or durable dispatch, and return per-action receipts and an updated report. Bundle:\n"+JSON.stringify({report_id:d.report_id,actions:chosen.map(a=>({action_id:a.action_id,project_id:a.project_id,title:a.title,route:a.route,risk:a.risk,prompt:a.prompt}))},null,2))};el("toggle").style.display="none";el("refresh").textContent="Refresh portfolio";el("refresh").onclick=()=>window.openai.callTool("repo_hermes_watch",{repo_id:d.repo_id,hermes_board:"portfolio-console",watch_seconds:10,poll_interval_seconds:5,max_events:1}).then(r=>render(r?.structuredContent||r));el("review").textContent="Ask ChatGPT for report";el("review").onclick=()=>follow("Review the portfolio action console. Summarize the most relevant verified work, stale evidence, blockers, and recommended next slices. Do not mutate anything without approval.")}
function renderBoard(){const list=tasks(),order=["triage","scheduled","todo","ready","running","proof_check","blocked","done"],seen=new Set(order),statuses=[...order,...list.map(t=>t.status).filter(s=>!seen.has(s))];el("board").innerHTML=statuses.filter(s=>list.some(t=>t.status===s)).map(status=>'<section class="column"><h3>'+esc(status)+' · '+list.filter(t=>t.status===status).length+'</h3><div class="cards">'+list.filter(t=>t.status===status).map(t=>'<button class="card '+(t.id===state.selected?'selected':'')+'" data-task="'+esc(t.id)+'"><div class="card-title">'+esc(t.title)+'</div><div class="card-meta"><span>'+esc(t.id)+'</span><span>'+esc(t.assignee||"unassigned")+'</span></div>'+(t.result_summary?'<div class="card-result">'+esc(t.result_summary)+'</div>':'')+'</button>').join("")+'</div></section>').join("")||'<div class="empty">No tasks returned for this board.</div>';el("board").querySelectorAll("[data-task]").forEach(card=>card.onclick=()=>{state.selected=card.dataset.task;renderBoard();renderInspector()})}
function renderTimeline(){const events=state.last?.events||[];el("timeline").innerHTML=events.length?events.map(e=>'<article class="event"><time>'+esc(new Date(e.observed_at).toLocaleTimeString())+' · '+esc(e.source)+'</time><div>'+esc(e.summary)+'</div></article>').join(""):'<div class="empty">No change. Heartbeat confirms the watcher is observing.</div>'}
function renderInspector(){const task=selected();el("taskTitle").textContent=task?.title||"Choose a Kanban card";el("taskMeta").textContent=task?[task.id,task.status,task.assignee||"unassigned",task.priority>=0?"priority "+task.priority:""].filter(Boolean).join(" · "):"Card controls use optimistic status checks and require connector approval.";el("assignee").value=task?.assignee||"";for(const id of ["diagnose","correct","comment","assign","block","schedule","recover","archive"]){el(id).disabled=!task||state.busy}el("archive").disabled=!task||task.status==="running"||task.status==="archived"||state.busy;el("correct").disabled=!task||!state.last?.hermes_transaction||state.busy;el("create").disabled=!state.last?.hermes_board||state.busy}
function buttons(){el("toggle").textContent=state.running?"Pause resident watch":"Start resident watch";el("toggle").disabled=state.busy;el("refresh").disabled=state.busy;renderInspector()}
async function watch(){if(state.busy||!window.openai?.callTool)return;state.busy=true;buttons();try{const previous=state.last?.request||{};const args={repo_id:previous.repo_id||"shared-agent-bridge",watch_seconds:45,poll_interval_seconds:10,max_events:12};if(previous.hermes_board)args.hermes_board=previous.hermes_board;if(previous.hermes_transaction)args.hermes_transaction=previous.hermes_transaction;if(state.cursor)args.hermes_cursor=state.cursor;const result=await window.openai.callTool("repo_hermes_watch",args);render(result?.structuredContent||result)}catch(error){receipt("Watch call failed. Use Observe now to retry.",false)}finally{state.busy=false;buttons()}if(state.running&&!state.last?.terminal)setTimeout(watch,250)}
function receipt(text,ok=true){el("receipt").className="receipt "+(ok?"ok":"error");el("receipt").textContent=text}
function renderPortfolioApp(d){
  state.last=d;
  const selectedActions=(d.actions||[]).filter(a=>state.portfolioSelected.has(a.action_id));
  const sectionLenses=(d.sections||[]).map(s=>'<span class="p-lens" title="'+esc((s.items||[]).slice(0,3).join(" · "))+'">'+esc(s.topic)+' · '+esc(s.headline)+'</span>').join("");
  const projectGroups=[];
  for(const action of d.actions||[]){let group=projectGroups.find(item=>item.id===action.project_id);if(!group){group={id:action.project_id,name:action.project_name,actions:[]};projectGroups.push(group)}group.actions.push(action)}
  const actionRow=a=>'<label class="p-action"><input type="checkbox" data-pick="'+esc(a.action_id)+'" '+(state.portfolioSelected.has(a.action_id)?'checked':'')+'><span><span class="p-action-title">'+esc(a.title)+'</span><span class="p-action-meta">'+esc(a.route)+' · '+esc(a.risk)+'</span></span></label>';
  const actionRows=projectGroups.map((group,index)=>{const picked=group.actions.filter(a=>state.portfolioSelected.has(a.action_id)).length;const body=group.actions.map(actionRow).join("");return group.actions.length>1?'<details class="p-group" '+(index===0||picked?'open':'')+'><summary><span>'+esc(group.name)+'</span><span class="p-group-count '+(picked?'p-group-selected':'')+'">'+picked+' selected · '+group.actions.length+' actions</span></summary><div class="p-group-body">'+body+'</div></details>':'<div class="p-single"><span class="p-single-label">'+esc(group.name)+' · 1 action</span>'+body+'</div>'}).join("");
  const queueRows=selectedActions.map(a=>'<div class="p-queued"><b>'+esc(a.project_name)+'</b> · '+esc(a.title)+'</div>').join("");
  const root=el("portfolioApp");el("kanbanApp").hidden=true;root.hidden=false;
  root.innerHTML='<main class="pfolio"><header class="p-head"><div><div class="eyebrow">Shared Agent Bridge · decision docket</div><h1>Portfolio action console</h1><div class="p-sub">'+esc(d.summary)+' · source '+esc(d.freshness)+' ('+esc(d.source_age_days)+' days)</div></div><button id="pExpand">Expand</button></header><div class="p-layout"><section class="p-panel p-overview"><div class="p-lenses">'+sectionLenses+'</div><div class="p-stats"><div class="p-stat"><b>'+Number((d.projects||[]).length)+'</b><span>projects</span></div><div class="p-stat"><b>'+Number((d.actions||[]).length)+'</b><span>actions</span></div><div class="p-stat"><b class="p-warning">'+Number((d.warnings||[]).length)+'</b><span>warnings</span></div></div></section><section class="p-panel p-action-panel"><div class="p-toolbar"><div><div class="eyebrow">Recommended actions</div><b>Choose one or several</b></div><button id="pSafe">Select read-only</button></div><div class="p-actions">'+(actionRows||'<div class="p-empty">No evidence-derived actions.</div>')+'</div></section><aside class="p-panel p-tray"><div class="p-toolbar"><div><div class="eyebrow">Decision tray</div><b>Queued actions</b></div><span class="p-count">'+selectedActions.length+'</span></div><div class="p-queue">'+(queueRows||'<div class="p-empty">Nothing selected yet.</div>')+'</div><div class="p-note">One bundle goes to ChatGPT. Each mutation still keeps its normal approval boundary.</div><div class="p-buttons"><button id="pClear">Clear</button><button id="pRefresh">Refresh</button><button id="pRoute" class="p-primary">Route selected</button></div><div id="pReceipt" class="p-receipt"></div></aside></div></main>';
  root.querySelectorAll("[data-pick]").forEach(box=>box.onchange=()=>{box.checked?state.portfolioSelected.add(box.dataset.pick):state.portfolioSelected.delete(box.dataset.pick);renderPortfolioApp(d)});
  el("pSafe").onclick=()=>{for(const a of d.actions||[])if(a.risk==="read_only")state.portfolioSelected.add(a.action_id);renderPortfolioApp(d)};
  el("pClear").onclick=()=>{state.portfolioSelected.clear();renderPortfolioApp(d)};
  document.getElementById("pExpand").onclick=()=>window.openai?.requestDisplayMode?.({mode:"fullscreen"});
  document.getElementById("pRefresh").onclick=async()=>{const r=await window.openai.callTool("repo_hermes_watch",{repo_id:d.repo_id,hermes_board:"portfolio-console",watch_seconds:10,poll_interval_seconds:5,max_events:1});render(r?.structuredContent||r)};
  document.getElementById("pRoute").onclick=()=>{const chosen=(d.actions||[]).filter(a=>state.portfolioSelected.has(a.action_id));if(!chosen.length){document.getElementById("pReceipt").textContent="Select at least one action first.";return}follow("Execute this portfolio decision bundle as one orchestrated batch. Validate current evidence for every project, group safe read-only checks, preserve approval boundaries for every mutation or durable dispatch, and return per-action receipts and an updated report. Bundle:\n"+JSON.stringify({report_id:d.report_id,actions:chosen.map(a=>({action_id:a.action_id,project_id:a.project_id,title:a.title,route:a.route,risk:a.risk,prompt:a.prompt}))},null,2))};
}
async function command(operation){const task=selected(),instruction=el("instruction").value.trim(),assignee=el("assignee").value.trim();if(operation!=="create_followup"&&!task)return receipt("Select a task first.",false);if(["comment","block","schedule","unblock","promote","archive"].includes(operation)&&!instruction)return receipt("Add an evidence-backed instruction or reason first.",false);const args={repo_id:"shared-agent-bridge",board:state.last.hermes_board,operation,dry_run:false};if(task){args.task_id=task.id;args.expected_status=task.status}if(instruction)args.instruction=instruction;if(assignee)args.assignee=assignee;if(operation==="block")args.block_kind=el("blockKind").value;if(operation==="create_followup"){const title=el("followTitle").value.trim();if(!title||!instruction||!assignee)return receipt("Follow-up title, body, and assignee are required.",false);args.title=title;args.body=instruction;args.idempotency_key="chatgpt:"+state.last.hermes_board+":"+title.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"").slice(0,90)}state.busy=true;buttons();receipt("Waiting for connector approval…");try{const result=await window.openai.callTool("repo_hermes_kanban_command",args);const d=result?.structuredContent||result;if(!d?.ok)return receipt((d?.warnings||["Command rejected."]).join(" · "),false);receipt(d.command_summary+" Verifying…");state.busy=false;buttons();await watch()}catch(error){receipt("Kanban command failed or was not approved.",false)}finally{state.busy=false;buttons()}}
async function correction(){const task=selected(),instruction=el("instruction").value.trim();if(!task||!instruction)return receipt("Select a card and enter the correction first.",false);state.busy=true;buttons();try{const result=await window.openai.callTool("repo_hermes_intervene",{repo_id:"shared-agent-bridge",transaction_id:state.last.hermes_transaction,intervention_type:"correction",instruction,reason:"Correction approved from the guarded Kanban console.",expected_evidence:"Hermes must record fresh task or artifact evidence addressing this correction."});const d=result?.structuredContent||result;if(!d?.ok)return receipt((d?.warnings||["Correction rejected."]).join(" · "),false);receipt("Correction receipt "+d.intervention_id+" recorded.");state.busy=false;buttons();await watch()}catch(error){receipt("Correction was not approved or could not be recorded.",false)}finally{state.busy=false;buttons()}}
el("toggle").onclick=()=>{state.running=!state.running;buttons();if(state.running)watch()};el("refresh").onclick=watch;el("review").onclick=()=>follow("Review the newest Hermes guarded-Kanban evidence shown in the widget. Report operational status, blocked cards, and the smallest safe next action. Do not mutate the board without explicit approval.");el("diagnose").onclick=()=>{const t=selected();if(t)follow("Diagnose Hermes Kanban task "+t.id+" on board "+state.last.hermes_board+". Current status: "+t.status+". Review its newest evidence, explain the likely problem, and propose one bounded correction. Do not mutate until I approve.")};el("correct").onclick=correction;el("comment").onclick=()=>command("comment");el("assign").onclick=()=>command("assign");el("block").onclick=()=>command("block");el("schedule").onclick=()=>command("schedule");el("recover").onclick=()=>{const t=selected();command(["blocked","scheduled"].includes(t?.status)?"unblock":"promote")};el("archive").onclick=()=>command("archive");el("create").onclick=()=>command("create_followup");
window.addEventListener("message",event=>{if(event.source!==window.parent)return;const d=event.data?.params?.structuredContent||event.data?.structuredContent;if(d?.watch_id||d?.report_id)render(d)},{passive:true});window.addEventListener("openai:set_globals",event=>{const d=event.detail?.globals?.toolOutput||toolOutput();if(d?.watch_id||d?.report_id)render(d)},{passive:true});const initial=toolOutput();if(initial?.watch_id||initial?.report_id)render(initial);else buttons();
</script></body></html>`;
}
